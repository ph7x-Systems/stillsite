[project]
name = "sardine-cms-build"
version = "0.2.0"
description = "Deterministic static site generator for Sardine CMS"
readme = "README.md"
requires-python = ">=3.12"
license = "Apache-2.0"
dependencies = ["sardine-cms-core>=0.2.0,<0.3", "sardine-cms-validation>=0.2.0,<0.3", "jinja2>=3.1", "markdown-it-py>=3.0"]

[project.optional-dependencies]
images = ["pillow>=10.4"]

[project.urls]
Homepage = "https://sardine.ph7x.com"
Repository = "https://github.com/ph7x-Systems/sardine-cms"
Documentation = "https://github.com/ph7x-Systems/sardine-cms/wiki"
Changelog = "https://github.com/ph7x-Systems/sardine-cms/blob/main/CHANGELOG.md"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/cms_build"]
